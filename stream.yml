spring:
  cloud:
    config:
      override-none: true
      override-system-properties: false

eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URI:http://localhost:8761/eureka}

logging:
  level:
    '[ROOT]': DEBUG
